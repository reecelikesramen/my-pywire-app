[project]
name = "my-pywire-app"
version = "0.1.0"
description = "A new pywire application"
requires-python = ">=3.12"
dependencies = [
    "pywire==0.2.7",
]

[tool.uv.sources]
pywire = { path = "../projects/pywire-monorepo/packages/pywire", editable = true }

